#pragma once
#include <GL/glew.h>

// J'avais pas de code pour sur le repo j'ai utilisé le miens en attendant pour les tests chez moi
GLuint LoadShaders(const char * vertex_file_path,const char * fragment_file_path);